<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.example.springmall.sample.mapper.SampleLoginMapper">
    <select id="login" resultType="int">
        SELECT COUNT(*) FROM sample WHERE sample_id=#{sampleId} AND sample_pw=#{samplePw};
    </select>
</mapper>